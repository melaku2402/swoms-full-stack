//allRoute // src/routes/academic-year.routes.js import express from "express"; import AcademicYearController from "../controllers/AcademicYearController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All academic year routes require authentication router.use(auth); // Get all academic years (admin, dean, department_head, registrar can view) router.get( "/", requireRole("admin", "dean", "department_head", "registrar"), AcademicYearController.getAllAcademicYears ); // Get active academic year router.get( "/active", requireRole("admin", "dean", "department_head", "registrar", "instructor"), AcademicYearController.getActiveAcademicYear ); // Get academic year by ID router.get( "/:id", requireRole("admin", "dean", "department_head", "registrar"), AcademicYearController.getAcademicYearById ); // Create academic year (admin and registrar only) router.post( "/", requireRole("admin", "registrar"), AcademicYearController.createAcademicYear ); // Update academic year (admin and registrar only) router.put( "/:id", requireRole("admin", "registrar"), AcademicYearController.updateAcademicYear ); // Delete academic year (admin only) router.delete( "/:id", requireRole("admin"), AcademicYearController.deleteAcademicYear ); // Activate academic year (admin and registrar only) router.put( "/:id/activate", requireRole("admin", "registrar"), AcademicYearController.activateAcademicYear ); // Get academic year statistics (admin, dean, department_head) router.get( "/:id/statistics", requireRole("admin", "dean", "department_head"), AcademicYearController.getAcademicYearStats ); export default router; import express from "express"; import AuthController from "../controllers/AuthController.js"; import { auth, requireRole } from "../middleware/auth.js"; import { ROLES } from "../config/constants.js"; const router = express.Router(); // PUBLIC ROUTES router.post("/login", AuthController.login); // PROTECTED ROUTES (All authenticated users) router.get("/profile", auth, AuthController.getProfile); router.put("/profile", auth, AuthController.updateProfile); router.put("/change-password", auth, AuthController.changePassword); router.post("/logout", auth, AuthController.logout); // USER MANAGEMENT ROUTES (Admin/Registrar/HR only) router.post( "/users", auth, requireRole("admin", "registrar", "hr_director"), AuthController.createUser ); router.get( "/users", auth, requireRole("admin", "registrar", "hr_director"), AuthController.getAllUsers ); router.get( "/users/:id", auth, requireRole("admin", "registrar", "hr_director"), (req, res) => { // Get specific user by ID (you'll need to implement this) res.json({ message: "Get user by ID - implement me" }); } ); router.put( "/users/:id", auth, requireRole("admin", "registrar", "hr_director"), AuthController.updateUser ); router.put( "/users/:id/deactivate", auth, requireRole("admin", "hr_director"), AuthController.deactivateUser ); router.put( "/users/:id/activate", auth, requireRole("admin", "hr_director"), AuthController.activateUser ); router.put( "/users/:id/reset-password", auth, requireRole("admin"), AuthController.resetPassword ); export default router; import express from "express"; import CollegeController from "../controllers/CollegeController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All college routes require authentication router.use(auth); // Get all colleges (admin, dean, department_head can view) router.get( "/", requireRole("admin", "dean", "department_head"), CollegeController.getAllColleges ); // Get college by ID router.get( "/:id", requireRole("admin", "dean", "department_head"), CollegeController.getCollegeById ); // Create college (admin only) router.post("/", requireRole("admin"), CollegeController.createCollege); // Update college (admin only) router.put("/:id", requireRole("admin"), CollegeController.updateCollege); // Delete college (admin only) router.delete("/:id", requireRole("admin"), CollegeController.deleteCollege); // Get college statistics router.get( "/:id/statistics", requireRole("admin", "dean"), CollegeController.getCollegeStats ); // Assign dean to college (admin only) router.post( "/:id/assign-dean", requireRole("admin"), CollegeController.assignDean ); export default router; // src/routes/course-assignment-full.routes.js import express from "express"; import CourseAssignmentController from "../controllers/CourseAssignmentController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All routes require authentication router.use(auth); // ==================== PROGRAM SELECTION ==================== router.get( "/programs", requireRole("department_head"), CourseAssignmentController.getProgramsForAssignment ); // ==================== PROGRAM STRUCTURE ==================== router.get( "/program/:programId/structure", requireRole("department_head"), CourseAssignmentController.getProgramStructure ); // ==================== COURSE ASSIGNMENT ==================== router.get( "/courses", requireRole("department_head"), CourseAssignmentController.getCoursesForAssignment ); router.post( "/assign", requireRole("department_head"), CourseAssignmentController.createAssignment ); router.post( "/assign/bulk", requireRole("department_head"), CourseAssignmentController.bulkAssignCourses ); // ==================== ASSIGNMENT MANAGEMENT ==================== router.get( "/assignments/year-levels", requireRole("department_head"), CourseAssignmentController.getAssignmentsByYearLevel ); router.get( "/assignments/overview", requireRole("department_head"), CourseAssignmentController.getAssignmentOverview ); // Assignment creation with overload checking router.post( "/", requireRole("department_head"), CourseAssignmentController.createAssignment ); // Check assignment feasibility without creating router.post( "/check-feasibility", requireRole("department_head"), CourseAssignmentController.checkAssignmentFeasibility ); // Get staff workload details router.get( "/staff/:id/workload", requireRole("department_head", "instructor", "admin"), CourseAssignmentController.getStaffWorkload ); // Keep existing routes... router.get( "/my", requireRole("instructor"), CourseAssignmentController.getMyAssignments ); router.get( "/department", requireRole("department_head"), CourseAssignmentController.getDepartmentAssignments ); router.get( "/availability", requireRole("department_head"), CourseAssignmentController.getStaffAvailability ); router.put( "/:id/accept", requireRole("instructor"), CourseAssignmentController.acceptAssignment ); router.put( "/:id/decline", requireRole("instructor"), CourseAssignmentController.declineAssignment ); router.put( "/:id/withdraw", requireRole("department_head"), CourseAssignmentController.withdrawAssignment ); router.get( "/:id", requireRole("admin", "department_head", "instructor"), CourseAssignmentController.getAssignmentById ); export default router; import express from "express"; import CourseRequestController from "../controllers/CourseRequestController.js"; import { auth as authenticate, requireRole as authorize, } from "../middleware/auth.js"; const router = express.Router(); // All routes require authentication router.use(authenticate); // Instructor routes router.post( "/", authorize("instructor"), CourseRequestController.createRequest ); router.get( "/my", authorize("instructor"), CourseRequestController.getMyRequests ); router.get( "/available", authorize("instructor"), CourseRequestController.getAvailableCourses ); router.put( "/:id/cancel", authorize("instructor"), CourseRequestController.cancelRequest ); // Department Head routes router.get( "/department", authorize("department_head"), CourseRequestController.getDepartmentRequests ); router.get( "/pending", authorize("department_head"), CourseRequestController.getPendingRequests ); router.put( "/:id/approve", authorize("department_head"), CourseRequestController.approveRequest ); router.put( "/:id/reject", authorize("department_head"), CourseRequestController.rejectRequest ); router.get( "/stats", authorize("department_head", "admin"), CourseRequestController.getRequestStats ); // Common routes router.get( "/:id", authorize("admin", "department_head", "instructor"), CourseRequestController.getRequestById ); export default router; // src/routes/course.routes.js import express from "express"; import CourseController from "../controllers/CourseController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All course routes require authentication router.use(auth); // Get all courses router.get( "/", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.getAllCourses ); // Search courses router.get( "/search", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.searchCourses ); // Get course by ID router.get( "/:id", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.getCourseById ); // Get courses by department router.get( "/department/:departmentId", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.getCoursesByDepartment ); // Get courses by program router.get( "/program/:programId", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.getCoursesByProgram ); // Get courses by program type router.get( "/type/:type", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.getCoursesByProgramType ); // Get course offerings router.get( "/:id/offerings", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.getCourseOfferings ); // Get course sections router.get( "/:id/sections", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.getCourseSections ); // Calculate course load router.get( "/:id/calculate-load", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.calculateCourseLoad ); // Get related courses router.get( "/:id/related", requireRole("admin", "dean", "department_head", "registrar", "instructor"), CourseController.getRelatedCourses ); // Get course statistics router.get("/statistics/overview", requireRole("admin", "dean", "department_head"), CourseController.getCourseStats ); // Create course (admin and registrar only) router.post( "/", requireRole("admin", "registrar"), CourseController.createCourse ); // Update course (admin and registrar only) router.put( "/:id", requireRole("admin", "registrar"), CourseController.updateCourse ); // Delete course (admin only) router.delete("/:id", requireRole("admin"), CourseController.deleteCourse); export default router; import express from "express"; import DepartmentController from "../controllers/DepartmentController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All department routes require authentication router.use(auth); // Get all departments router.get( "/", requireRole("admin", "dean", "department_head"), DepartmentController.getAllDepartments ); // Get departments by college router.get( "/college/:collegeId", requireRole("admin", "dean", "department_head"), DepartmentController.getDepartmentsByCollege ); // Get department by ID router.get( "/:id", requireRole("admin", "dean", "department_head", "instructor"), DepartmentController.getDepartmentById ); // Create department (admin only) router.post("/", requireRole("admin"), DepartmentController.createDepartment); // Update department (admin and department head of that department) router.put( "/:id", requireRole("admin", "department_head"), DepartmentController.updateDepartment ); // Delete department (admin only) router.delete( "/:id", requireRole("admin"), DepartmentController.deleteDepartment ); // Get department statistics router.get( "/:id/statistics", requireRole("admin", "dean", "department_head"), DepartmentController.getDepartmentStats ); // Assign head to department (admin and dean) router.post( "/:id/assign-head", requireRole("admin", "dean"), DepartmentController.assignHead ); export default router; // src/routes/index.js - CLEANED VERSION import express from "express"; import authRoutes from "./auth.routes.js"; import collegeRoutes from "./college.routes.js"; import departmentRoutes from "./department.routes.js"; import academicYearRoutes from "./academic-year.routes.js"; import semesterRoutes from "./semester.routes.js"; import staffRoutes from "./staff.routes.js"; import programRoutes from "./program.routes.js"; import courseRoutes from "./course.routes.js"; import sectionRoutes from "./section.routes.js"; import workloadRPRoutes from "./workload-rp.routes.js"; import workloadNRPRoutes from "./workload-nrp.routes.js"; import rulesRoutes from "./rules.routes.js"; import roleRegistrationRoutes from "./role-registration.routes.js"; import courseAssignmentRoutes from "./course-assignment.routes.js"; import courseRequestRoutes from "./course-request.routes.js"; import overloadDetectionRoutes from "./overload-detection.routes.js"; const router = express.Router(); // API Documentation router.get("/", (req, res) => { res.json({ message: "SWOMS API v2.0", university: "Injibara University", endpoints: { auth: "/api/auth", colleges: "/api/colleges", departments: "/api/departments", academic_years: "/api/academic-years", semesters: "/api/semesters", staff: "/api/staff", programs: "/api/programs", courses: "/api/courses", sections: "/api/sections", workload: { regular: "/api/workload/rp", npr: "/api/workload/nrp", }, rules: "/api/rules", role_registration: "/api/role-registration", course_assignments: "/api/course-assignments", course_requests: "/api/course-requests", overload_detection: "/api/overload-detection", }, }); }); // Health check router.get("/health", (req, res) => { res.json({ status: "healthy", timestamp: new Date().toISOString(), uptime: process.uptime(), }); }); // Mount routes router.use("/auth", authRoutes); router.use("/colleges", collegeRoutes); router.use("/departments", departmentRoutes); router.use("/academic-years", academicYearRoutes); router.use("/semesters", semesterRoutes); router.use("/staff", staffRoutes); router.use("/programs", programRoutes); router.use("/courses", courseRoutes); router.use("/sections", sectionRoutes); router.use("/workload/rp", workloadRPRoutes); router.use("/workload/nrp", workloadNRPRoutes); router.use("/rules", rulesRoutes); router.use("/role-registration", roleRegistrationRoutes); router.use("/course-assignments", courseAssignmentRoutes); router.use("/course-requests", courseRequestRoutes); router.use("/overload-detection", overloadDetectionRoutes); export default router; import express from "express"; import OverloadDetectionController from "../controllers/OverloadDetectionController.js"; // import { authenticate, authorize } from "../middleware/auth.js"; import { auth as authenticate, requireRole as authorize, } from "../middleware/auth.js"; const router = express.Router(); // All routes require authentication router.use(authenticate); // Instructor routes router.get("/my", authorize("instructor"), OverloadDetectionController.checkMyOverload); // Department Head routes router.get("/department/:departmentId/:semesterId", authorize("department_head", "admin", "hr_director", "dean"), OverloadDetectionController.checkDepartmentOverload); router.get("/alerts", authorize("department_head", "admin", "hr_director", "dean"), OverloadDetectionController.getOverloadAlerts); // Admin/HR/Dean routes router.get("/staff/:staffId/:semesterId", authorize("admin", "hr_director", "dean", "department_head"), OverloadDetectionController.checkStaffOverload); router.post("/predict/:staffId", authorize("admin", "hr_director", "dean", "department_head"), OverloadDetectionController.predictOverloadTrend); router.get("/report/:staffId/:semesterId", authorize("admin", "hr_director", "department_head"), OverloadDetectionController.generateOverloadReport); export default router; // ============================================ // FILE: src/routes/payment.routes.js // ============================================ import express from "express"; import PaymentController from "../controllers/PaymentController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All payment routes require authentication router.use(auth); // Get all payment sheets router.get( "/sheets", requireRole("admin", "finance", "cde_director", "vpaf", "hr_director"), PaymentController.getAllPaymentSheets ); // Get payment sheet by ID router.get( "/sheets/:id", requireRole( "admin", "finance", "cde_director", "vpaf", "hr_director", "instructor" ), PaymentController.getPaymentSheetById ); // Update payment status router.put( "/sheets/:id/status", requireRole("admin", "finance"), PaymentController.updatePaymentStatus ); // Export payment sheet router.get( "/sheets/:id/export", requireRole("admin", "finance", "cde_director"), PaymentController.exportPaymentSheet ); // Get payment statistics router.get( "/statistics", requireRole("admin", "finance", "vpaf", "cde_director"), PaymentController.getPaymentStatistics ); // Bulk update payment status router.post( "/bulk-update", requireRole("admin", "finance"), PaymentController.bulkUpdatePaymentStatus ); export default router; // src/routes/program.routes.js import express from "express"; import ProgramController from "../controllers/ProgramController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All program routes require authentication router.use(auth); // Get all programs router.get( "/", requireRole("admin", "dean", "department_head", "registrar", "instructor"), ProgramController.getAllPrograms ); // Get program by ID router.get( "/:id", requireRole("admin", "dean", "department_head", "registrar", "instructor"), ProgramController.getProgramById ); // Get programs by department router.get( "/department/:departmentId", requireRole("admin", "dean", "department_head", "registrar"), ProgramController.getProgramsByDepartment ); // Get programs by type router.get( "/type/:type", requireRole("admin", "dean", "department_head", "registrar"), ProgramController.getProgramsByType ); // Get program types router.get( "/types/all", requireRole("admin", "dean", "department_head", "registrar"), ProgramController.getProgramTypes ); // Get program statistics router.get( "/statistics/overview", requireRole("admin", "dean", "department_head"), ProgramController.getProgramStats ); // Get program types dashboard router.get( "/dashboard/types", requireRole("admin", "dean"), ProgramController.getProgramTypesDashboard ); // Create program (admin and registrar only) router.post( "/", requireRole("admin", "registrar"), ProgramController.createProgram ); // Update program (admin and registrar only) router.put( "/:id", requireRole("admin", "registrar"), ProgramController.updateProgram ); // Delete program (admin only) router.delete("/:id", requireRole("admin"), ProgramController.deleteProgram); // Assign courses to program router.post( "/:id/assign-courses", requireRole("admin", "registrar"), ProgramController.assignCourses ); export default router; import express from "express"; import RoleRegistrationController from "../controllers/RoleRegistrationController.js"; import { auth, requireRole } from "../middleware/auth.js"; import { ALL_ROLES } from "../config/constants.js"; // Import ALL_ROLES const router = express.Router(); // All routes require authentication router.use(auth); // ===== ROLE-BASED USER CREATION ===== // Create user following Phase 1 hierarchy (Generic endpoint) router.post( "/create", requireRole("admin", "vpaa", "dean", "hr_director"), RoleRegistrationController.createUserByRole ); // Get users I have created router.get( "/my-created-users", requireRole("admin", "vpaa", "dean", "hr_director"), RoleRegistrationController.getMyCreatedUsers ); // Get users by specific role router.get( "/role/:role", requireRole("admin", "vpaa", "hr_director", "dean", "department_head"), RoleRegistrationController.getUsersByRole ); // ===== PERMISSIONS AND STATUS ===== // Get my role creation permissions router.get("/permissions", auth, RoleRegistrationController.getMyPermissions); // Get admin creatable roles router.get( "/admin/creatable-roles", requireRole("admin"), RoleRegistrationController.getAdminCreatableRoles ); // ===== SPECIFIC ROLE CREATION ENDPOINTS ===== // Admin creates ANY role (no restrictions) router.post( "/admin/create-executives", requireRole("admin"), RoleRegistrationController.createUserByRole ); // VPAA creates Deans and Registrar router.post( "/vpaa/create-deans-registrar", requireRole("vpaa"), (req, res, next) => { req.body.role = req.body.role || ""; if (!["dean", "registrar"].includes(req.body.role)) { return res.status(400).json({ success: false, message: "VPAA can only create: dean, registrar", }); } next(); }, RoleRegistrationController.createUserByRole ); // Dean creates Department Heads router.post( "/dean/create-department-heads", requireRole("dean"), (req, res, next) => { req.body.role = "department_head"; // Force role to department_head next(); }, RoleRegistrationController.createUserByRole ); // HR Director creates Instructors router.post( "/hr/create-instructors", requireRole("hr_director"), (req, res, next) => { req.body.role = "instructor"; // Force role to instructor next(); }, RoleRegistrationController.createUserByRole ); export default router; import express from "express"; import RulesController from "../controllers/RulesController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All routes require authentication router.use(auth); // ==================== CRUD ROUTES ==================== // Get all rules (Admin, Finance, HR, Dean, Department Head) router.get( "/", requireRole("admin", "finance", "hr_director", "dean", "department_head"), RulesController.getAllRules ); // Get rule by ID router.get( "/:id", requireRole("admin", "finance", "hr_director", "dean", "department_head"), RulesController.getRuleById ); // Create rule (Admin and Finance only) router.post("/", requireRole("admin", "finance"), RulesController.createRule); // Update rule (Admin and Finance only) router.put("/:id", requireRole("admin", "finance"), RulesController.updateRule); // Delete rule (Admin only) router.delete("/:id", requireRole("admin"), RulesController.deleteRule); // Activate rule router.post( "/:id/activate", requireRole("admin", "finance"), RulesController.activateRule ); // Deactivate rule router.post( "/:id/deactivate", requireRole("admin", "finance"), RulesController.deactivateRule ); // ==================== BUSINESS LOGIC ROUTES ==================== // Evaluate rule (All authenticated users) router.post("/evaluate", auth, RulesController.evaluateRule); // Calculate load router.post("/calculate-load", auth, RulesController.calculateLoad); // Validate rank load router.post("/validate-rank-load", auth, RulesController.validateRankLoad); // Calculate NRP payment router.post( "/calculate-nrp-payment", auth, RulesController.calculateNRPPayment ); // Calculate overload payment router.post( "/calculate-overload-payment", auth, RulesController.calculateOverloadPayment ); // Get correction rates router.get("/correction-rates", auth, RulesController.getCorrectionRates); // Apply summer distribution router.post( "/apply-summer-distribution", requireRole("admin", "finance", "cde_director"), RulesController.applySummerDistribution ); // ==================== QUERY ROUTES ==================== // Get rule by name router.get("/name/:name", auth, RulesController.getRuleByName); // Get rules by type router.get("/type/:type", auth, RulesController.getRulesByType); // Get rule history router.get( "/history/:name", requireRole("admin", "finance", "hr_director", "dean"), RulesController.getRuleHistory ); // Get rank load limits router.get("/rank-limits/:rank", auth, RulesController.getRankLoadLimits); // Get all rank load limits router.get("/rank-limits", auth, RulesController.getAllRankLoadLimits); // Get load factors router.get("/load-factors", auth, RulesController.getLoadFactors); // Get payment rates router.get( "/payment-rates/:program_type", auth, RulesController.getPaymentRates ); // Get summer distribution router.get("/summer-distribution", auth, RulesController.getSummerDistribution); // ==================== UTILITY ROUTES ==================== // Validate workload router.post("/validate-workload", auth, RulesController.validateWorkload); // Bulk update rules router.post( "/bulk-update", requireRole("admin", "finance"), RulesController.bulkUpdateRules ); // Get rules dashboard router.get( "/dashboard/summary", requireRole("admin", "finance", "hr_director", "dean"), RulesController.getRulesDashboard ); // Get active rate tables router.get("/rate-tables/active", auth, RulesController.getActiveRateTables); // Get active tax rules router.get("/tax-rules/active", auth, RulesController.getActiveTaxRules); // Clear cache (Admin only) router.post("/cache/clear", requireRole("admin"), RulesController.clearCache); export default router; // src/routes/section.routes.js import express from "express"; import SectionController from "../controllers/SectionController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All section routes require authentication router.use(auth); // Get all sections router.get( "/", requireRole("admin", "dean", "department_head", "registrar", "instructor"), SectionController.getAllSections ); // Get section by ID router.get( "/:id", requireRole("admin", "dean", "department_head", "registrar", "instructor"), SectionController.getSectionById ); // Get sections by course and semester router.get( "/course/:courseId/semester/:semesterId", requireRole("admin", "dean", "department_head", "registrar", "instructor"), SectionController.getSectionsByCourseAndSemester ); // Get sections by instructor router.get( "/instructor/:instructorId", requireRole("admin", "dean", "department_head", "registrar", "instructor"), SectionController.getSectionsByInstructor ); // Get sections by department router.get( "/department/:departmentId", requireRole("admin", "dean", "department_head", "registrar"), SectionController.getSectionsByDepartment ); // Get unassigned sections router.get( "/unassigned/list", requireRole("admin", "dean", "department_head", "registrar"), SectionController.getUnassignedSections ); // Get sections by date range router.get( "/date-range/filter", requireRole("admin", "dean", "department_head", "registrar"), SectionController.getSectionsByDateRange ); // Get section statistics router.get( "/statistics/overview", requireRole("admin", "dean", "department_head"), SectionController.getSectionStats ); // Get dashboard summary router.get( "/dashboard/summary", requireRole("admin", "dean", "department_head"), SectionController.getDashboardSummary ); // Create section (admin and registrar only) router.post( "/", requireRole("admin", "registrar"), SectionController.createSection ); // Update section (admin and registrar only) router.put( "/:id", requireRole("admin", "registrar"), SectionController.updateSection ); // Delete section (admin only) router.delete("/:id", requireRole("admin"), SectionController.deleteSection); // Assign instructor to section router.post( "/:id/assign-instructor", requireRole("admin", "registrar", "department_head"), SectionController.assignInstructor ); // Remove instructor from section router.delete( "/:id/remove-instructor", requireRole("admin", "registrar", "department_head"), SectionController.removeInstructor ); // Update student count router.put( "/:id/student-count", requireRole("admin", "registrar", "department_head"), SectionController.updateStudentCount ); export default router; // src/routes/semester.routes.js import express from "express"; import SemesterController from "../controllers/SemesterController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All semester routes require authentication router.use(auth); // Get all semesters (admin, dean, department_head, registrar can view) router.get( "/", requireRole("admin", "dean", "department_head", "registrar"), SemesterController.getAllSemesters ); // Get active semester router.get( "/active", requireRole("admin", "dean", "department_head", "registrar", "instructor"), SemesterController.getActiveSemester ); // Get current semester (based on date) router.get( "/current", requireRole("admin", "dean", "department_head", "registrar", "instructor"), SemesterController.getCurrentSemester ); // Get semester by ID router.get( "/:id", requireRole("admin", "dean", "department_head", "registrar", "instructor"), SemesterController.getSemesterById ); // Get semesters by academic year router.get( "/academic-year/:academicYearId", requireRole("admin", "dean", "department_head", "registrar"), SemesterController.getSemestersByAcademicYear ); // Create semester (admin and registrar only) router.post( "/", requireRole("admin", "registrar"), SemesterController.createSemester ); // Update semester (admin and registrar only) router.put( "/:id", requireRole("admin", "registrar"), SemesterController.updateSemester ); // Delete semester (admin only) router.delete("/:id", requireRole("admin"), SemesterController.deleteSemester); // Activate semester (admin and registrar only) router.put( "/:id/activate", requireRole("admin", "registrar"), SemesterController.activateSemester ); // Get semester statistics (admin, dean, department_head) router.get( "/:id/statistics", requireRole("admin", "dean", "department_head"), SemesterController.getSemesterStats ); export default router; // src/routes/staff.routes.js import express from "express"; import StaffController from "../controllers/StaffController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All staff routes require authentication router.use(auth); // ===== PUBLIC STAFF ROUTES (for instructors to view their own data) ===== // Get my staff profile router.get( "/me", requireRole( "instructor", "department_head", "dean", "cde_director", "hr_director", "vpaa", "vpaf" ), StaffController.getMyProfile ); // Add to staff.routes.js router.get("/me/profile-check", auth, StaffController.checkMyStaffProfile); // Update my profile router.put( "/me", requireRole( "instructor", "department_head", "dean", "cde_director", "hr_director", "vpaa", "vpaf" ), StaffController.updateMyProfile ); // Get my workload summary router.get( "/me/workload-summary", requireRole( "instructor", "department_head", "dean", "cde_director", "hr_director", "vpaa", "vpaf" ), StaffController.getStaffWorkloadSummary ); // ===== ADMIN/MANAGEMENT ROUTES ===== // Get all staff with filters router.get( "/", requireRole("admin", "registrar", "hr_director", "dean", "department_head"), StaffController.getAllStaff ); // Search staff router.get( "/search", requireRole("admin", "registrar", "hr_director", "dean", "department_head"), StaffController.searchStaff ); // Get staff statistics router.get( "/statistics", requireRole("admin", "hr_director", "dean"), StaffController.getStaffStatistics ); // Create new staff (admin, registrar, HR only) router.post( "/", requireRole("admin", "registrar", "hr_director"), StaffController.createStaff ); // ===== SPECIFIC STAFF ROUTES ===== // Get staff by ID router.get( "/:id", requireRole("admin", "registrar", "hr_director", "dean", "department_head"), StaffController.getStaffById ); // Update staff router.put( "/:id", requireRole("admin", "registrar", "hr_director"), StaffController.updateStaff ); // Delete staff (soft delete) router.delete( "/:id", requireRole("admin", "hr_director"), StaffController.deleteStaff ); // Get staff by user ID router.get( "/user/:userId", requireRole("admin", "registrar", "hr_director"), StaffController.getStaffByUserId ); // Get staff by department router.get( "/department/:departmentId", requireRole("admin", "registrar", "hr_director", "dean", "department_head"), StaffController.getStaffByDepartment ); // Get staff workload summary router.get( "/:id/workload-summary", requireRole("admin", "hr_director", "dean", "department_head"), StaffController.getStaffWorkloadSummary ); // Update staff rank router.put( "/:id/rank", requireRole("admin", "hr_director"), StaffController.updateStaffRank ); // Assign staff to department router.put( "/:id/assign-department", requireRole("admin", "hr_director", "dean"), StaffController.assignToDepartment ); // Get staff by rank router.get( "/rank/:rank", requireRole("admin", "hr_director", "dean", "department_head"), StaffController.getStaffByRank ); export default router; // ============================================ // FILE: src/routes/workload-nrp.routes.js (Simplified) // ============================================ import express from "express"; import WorkloadNRPController from "../controllers/WorkloadNRPController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All routes require authentication router.use(auth); // ===== CREATE ===== router.post( "/", requireRole("instructor", "department_head", "dean", "admin", "cde_director"), WorkloadNRPController.createNRPWorkload ); // ===== READ ===== // Get all NRP workloads (role-based filtering in controller) router.get( "/", requireRole( "instructor", "department_head", "dean", "admin", "registrar", "cde_director", "hr_director", "vpaf", "finance" ), WorkloadNRPController.getAllNRPWorkloads ); // Get single NRP workload router.get( "/:id", requireRole( "instructor", "department_head", "dean", "admin", "registrar", "cde_director", "hr_director", "vpaf", "finance" ), WorkloadNRPController.getNRPWorkloadById ); // ===== UPDATE ===== router.put( "/:id", requireRole("instructor", "department_head", "dean", "admin"), WorkloadNRPController.updateNRPWorkload ); // ===== SUBMIT FOR APPROVAL ===== router.post( "/:id/submit", requireRole("instructor"), WorkloadNRPController.submitForApproval ); // ===== STATISTICS ===== router.get( "/statistics/dashboard", requireRole("admin", "dean", "department_head", "cde_director"), WorkloadNRPController.getNRPStatistics ); export default router; // export default router; import express from "express"; import WorkloadRPController from "../controllers/WorkloadRPController.js"; import { auth, requireRole } from "../middleware/auth.js"; const router = express.Router(); // All workload routes require authentication router.use(auth); // ===== INSTRUCTOR ROUTES ===== // Get my workload router.get( "/me", requireRole("instructor", "department_head", "dean"), WorkloadRPController.getMyWorkload ); // Get my workload dashboard router.get( "/me/dashboard", requireRole("instructor", "department_head", "dean"), WorkloadRPController.getWorkloadDashboard ); // Create or update my workload router.post( "/me", requireRole("instructor", "department_head", "dean"), WorkloadRPController.createOrUpdateWorkload ); // Calculate workload from my sections router.post( "/me/calculate-from-sections", requireRole("instructor", "department_head", "dean"), WorkloadRPController.calculateFromSections ); // Submit my workload for approval router.post( "/me/:id/submit", requireRole("instructor", "department_head", "dean"), WorkloadRPController.submitForApproval ); // ===== APPROVAL WORKFLOW ROUTES ===== // Get my pending approvals router.get( "/approvals/pending", requireRole( "department_head", "dean", "hr_director", "vpaa", "vpaf", "finance" ), WorkloadRPController.getMyPendingApprovals ); // Get approval workflow for a workload router.get( "/:id/approvals", requireRole( "admin", "department_head", "dean", "hr_director", "vpaa", "vpaf", "finance", "instructor" ), WorkloadRPController.getApprovalWorkflow ); // Approve a step router.post( "/approvals/:approvalId/approve", requireRole( "department_head", "dean", "hr_director", "vpaa", "vpaf", "finance" ), WorkloadRPController.approveStep ); // Reject a workload router.post( "/:id/reject", requireRole( "department_head", "dean", "hr_director", "vpaa", "vpaf", "finance" ), WorkloadRPController.rejectWorkload ); // Return workload for correction router.post( "/:id/return", requireRole( "department_head", "dean", "hr_director", "vpaa", "vpaf", "finance" ), WorkloadRPController.returnForCorrection ); // ===== MANAGEMENT ROUTES ===== // Get all workloads (with filters) - ALLOW INSTRUCTORS router.get( "/", requireRole( "admin", "dean", "department_head", "hr_director", "vpaa", "vpaf", "finance", "registrar", "instructor" ), WorkloadRPController.getAllWorkloads ); // Get workload by ID router.get( "/:id", requireRole( "admin", "dean", "department_head", "hr_director", "vpaa", "vpaf", "finance", "instructor" ), WorkloadRPController.getWorkloadById ); // Get workload statistics router.get( "/statistics/overview", requireRole("admin", "dean", "department_head", "hr_director"), WorkloadRPController.getWorkloadStatistics ); // Get department workload summary router.get( "/department/:departmentId/summary", requireRole("admin", "dean", "department_head", "hr_director"), WorkloadRPController.getDepartmentWorkloadSummary ); // Create or update workload (admin/HR can create for others) router.post( "/", requireRole("admin", "hr_director"), WorkloadRPController.createOrUpdateWorkload ); // Calculate workload from sections (admin/HR/dept head) router.post( "/calculate-from-sections", requireRole("admin", "hr_director", "department_head"), WorkloadRPController.calculateFromSections ); // Delete workload (admin only) router.delete( "/:id", requireRole("admin"), WorkloadRPController.deleteWorkload ); //save this route all full functional and add to my controler and ready to the next question